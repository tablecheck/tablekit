import styled from '@emotion/styled';

export const HeaderRow = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: var(--spacing-l1);
  position: relative;
`;
