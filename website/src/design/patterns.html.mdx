import Layout from '../layout/index.html';

export default function (props) {
  return (
    <Layout
      title="Patterns"
      sideNav="design"
      editPageRoute="design/patterns.html.mdx"
    >
      {props.children}
    </Layout>
  );
}

# Patterns

[Empty States](/design/patterns/empty-states)

Empty states should have a consistent structure across TableCheck products.

[Spacing](/design/patterns/spacing)

Spacing should have a consistent structure across TableCheck products.
